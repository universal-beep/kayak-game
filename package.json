{
  "name": "kayak-game",
  "version": "1.0.0",
  "private": true,
  "description": "«Сплав на байдарке» — одностраничная HTML-игра (canvas). Тесты и линтер работают через node:vm на mock-DOM.",
  "scripts": {
    "lint": "node scripts/lint.mjs",
    "test": "node --test tests/*.test.mjs",
    "test:affected": "node --test tests/*.test.mjs",
    "analyze": "node scripts/analyze.mjs"
  }
}