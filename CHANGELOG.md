# Сплав на байдарке — журнал изменений

Журнал собирается из git-истории репозитория. Каждое изменение фиксируется отдельным коммитом в формате Conventional Commits (`feat`, `fix`, `docs`, `chore`). Полная история — в `git log`.

История: 81 коммитов, 2026-08-06 — 2026-09-02.

## Изменения

### 2026-08-06
- **Служебное:** тестовый стенд для игры и спека с планом переработки
- **Возможности:** осевая линия русла, ширина и кривизна
- **Возможности:** мировые координаты и поперечная координата t
- **Возможности:** генератор сегментов плёс/порог
- **Возможности:** сглаженный переход между плёсом и порогом
- **Возможности:** построчный рендер изгибающегося русла
- **Исправления:** река теперь поворачивает, а не меняет ширину
- **Возможности:** инерция, стрежень и снос течением
- **Исправления:** пересчитать снос под новую кривизну русла
- **Возможности:** препятствия, бонусы и берега в координатах русла
- **Возможности:** пиксельные спрайты, прочность и четыре новых бонуса
- **Возможности:** широкая река, музыка, три попытки, результаты сессии

### 2026-08-07
- **Возможности:** экран продолжения, живой берег, плывущие брёвна, звуки
- **Возможности:** четыре реки с характерами, комбо за проход впритирку, обгоны
- **Возможности:** драка вёслами с парированием по таймингу
- **Возможности:** каркас кампании — девять дней, финиш, звёзды, карта, сохранение
- **Возможности:** пауза
- **Возможности:** Волга без порогов, финишная лента с причаливанием, имена и таблица
- **Возможности:** лагерь, магазин снаряжения и бардовский вальс у костра
- **Возможности:** развилки-острова, дождь, мосты, баржи и топляк
- **Возможности:** четыре микромульта — ночёвка, утро, переезд и финал с электричкой
- **Исправления:** коряга и развилки
- **Исправления:** излом русла, потеря четвёртого сердца, палатки вместо церкви
- **Исправления:** шрифт внутрь файла, виски удваивает очки, баржа пробивает щит
- **Исправления:** комбо заработало, сложность перебалансирована, мосты размечены
- **Возможности:** настройка музыки, звук паузы, компания с гитарой, понятные бонусы
- **Исправления:** баржа шла поперёк реки

### 2026-08-08
- **Возможности:** продольная гребля — разгон и торможение
- **Возможности:** байдарка ездит по экрану, имя спрашивается один раз
- **Исправления:** при гребле уезжала вся картина, а не лодка; мост не читался

### 2026-08-09
- **Исправления:** байдарка плыла по мосту
- **Исправления:** полоса порога теперь подписана

### 2026-08-10
- **Исправления:** закрыты долги — пауза, версия сохранения, звёзды, фарм, итоги, мёртвый код

### 2026-08-30
- **Исправления:** по итогам playtest-критики — имя/результаты, объезд агрессора, отклик удара, ввод (Enter/repeat/blur/RU), пробы red-green
- **Исправления:** снаряжение из лагеря теперь видно — бейджи в HUD, тост покупки, 'в рюкзаке' в магазине, пробы на механику
- **Исправления:** итерация 2 — день 9 без дубля третьей звезды, анти-фарм за повтор дней, итоги похода на карте, мобильный ландшафт (кнопки по углам + safe-area)
- **Исправления:** мобила — тап после руления больше не бьёт веслом (tapMoved), экраны скроллятся на маленьких девайсах (safe center + pan-y)
- **feat(vis):** глубина мира — туман вдали создаёт горизонт, кусты/деревья/камыш по берегам, выпуклая вода
- **feat(mobile):** управление без кнопок — скорость зонами экрана, удар двойным тапом, обучающий оверлей
- **fix(cutscene):** в заставке drive лодка — груз на крыше машины, а не «вдогонку по воздуху» (3 красные пробы → зелёные)
- **feat(cutscene):** полноэкранные заставки — канвас на весь экран через cutFull/rs(), расклад cutScr (масштаб по высоте, сцена по центру), подпись внизу; 3 пробы на cutScr и рисование drwCut (red-green)
- **feat(preview):** режим просмотра катсцен через ?cut=night|morning|drive|finale|all — без прохождения игры
- **Исправления:** 4 долга по феел-честности и управлению (red-green, 77->81 тестов)

### 2026-08-31
- **Возможности:** катсцена night живая — мерцающий костёр, искры, турист в палатку
- **Исправления:** долги E5-E8 — анти-фарм провалов, пауза hold-гейтом, обрыв жеста, тени и посадка поезда в катсценах
- **Исправления:** E9-наброски — птицы/дым в morning, sfx splash/bell, бонус-солнце, спасажилет
- **Возможности:** E9-доделка — кат-сцена под мостом (первый чистый пролёт дня) и ветка-препятствие
- **Возможности:** редизайн катсцен — лодки профилем, прицеп за машиной, мост-конструкция, кулисы против пустоты
- **Исправления:** откат перегруза катсцен — убраны кулисы и россыпь деталей, оставлены точечные правки фидбека (профиль-лодка, прицеп, собранный мост)
- **finale:** поезд выше человека (баг масштаба) — локомотив+3 вагона 96x18 + красная проба пропорций
- **finale:** паровоз едет первым (голова вправо по ходу) + 4 вагона вместо 3
- **finale:** 9 вагонов, сценарий остановки (человек исчезает при отправлении), ж/д пути внизу
- **finale:** поезд останавливается у человека на 3-4 вагоне (человек поверх, станция x=70)
- **finale:** человек за вагонами, вокзал ближе и больше (scl=2)
- **катсцены:** адаптивность под мобильные — fit масштаб (s=min(cw/420,ch/720))
- **катсцены:** убрать плашку-подсказку «нажми, чтобы пропустить»
- **finale:** деревья вокруг вокзала и больше пассажиров на перроне
- **finale:** пассажиры одного роста и в разной одежде (var), город-силуэт за вокзалом
- **finale:** тротуар-плитка перед вокзалом; одежда пассажиров через var (проверка)
- **finale:** баг — одежда пассажиров не менялась (drawImage рисовал spr.img вместо img); поезд опущен на рельсы
- **finale:** локомотив как первый вагон; вокзал ниже к перрону; +2 ряда рельсов с отступом
- **finale:** головной вагон энергичный и контрастный (красный+жёлтый+белая молния), паровозность убрана
- **finale:** зелень+отступ между нижними рельсами и ещё 2 линии (6 путей)
- **drive:** дорога для машины (асфальт+разметка) и жёлтая байдарка-профиль
- **drive:** 4-полосная дорога шире и ниже (зелёная трава сверху), белая осевая, машины по середине полос, порядок по глубине 1-4 (полоса 4 всегда видна), крупные машины scl 1.3 (bus/kamaz 1.73), медленный грузовой транспорт, машина дольше в центре; журнал и публикация
- **night:** байдарки кучнее у кромки воды, лес елей плотнее (5->9 fir), лагерь из 7 палаток двух форм (tent + новый купольный tent2) и семи цветов (variants swapPalette); тест байдарка-на-берегу переписан под kayak_side и новую сцену; журнал
- **night:** убрал тень-эллипсы в ночной сцене (чёрные ромбы у палаток на тёмном лугу), тест теней переписан (drive=есть, night=0); байдарки чуть раздвинул; журнал
- **night:** ярусная ель вместо сплошного ромба + плотная лесная кромка (16 елей, мельче); mHint скрывается на мультах (playCut -> showMobileHint(false))
- **night:** hero visible from start, standing people sleep after his leave

### 2026-09-01
- **feat(night):** гитарист одним спрайтом + встроенный звук ночёвки (3 mp3), длина сцены per-scene
- **feat(night):** единый трек ночёвки v2 (подрезанная пауза, без финальной тишины) + хвост сцены и плавное затемнение в чёрное (len 780->1020, 3с тихого лагеря + 1с фейд); cutFade+drwCut, тесты
- **Документация:** журнал изменений и описание игры
- **fix(timing):** фиксированный шаг 60Гц вместо кадрового цикла — звук ночи и катсцены идут синхронно на мобиле (30fps); simPlan-аккумулятор + lp на реальном времени, 2 красные пробы (30==60, ночь за 17с)

### 2026-09-02
- **Документация:** журнал изменений (фикс тайминга 60Гц)
- **fix(mobile):** подсказка «как двигаться» (#mHint) опущена под указатель реки — оверлей и знак уровня больше не накладываются на старте дня (top 14%->32%)
- **Документация:** журнал изменений (фикс наложения mHint на указатель реки)
- **fix(art):** бревно теперь бокового вида — круглые торцы с кольцами и кора кверху-книзу (читается как бревно, а не диск); размер спрайта не изменился
- **Документация:** журнал изменений (бревно бокового вида)
- **fix(art):** бревно — больше коры вдоль тела и «комлевой» закруглённый конец слева с годовыми кольцами; габариты спрайта прежние
- **Документация:** журнал изменений (бревно больше коры и комлевой конец)
- **fix(art):** бревно — откат к первому боковому виду (круглые торцы с кольцами по обоим краям); рисую свой вариант сам

### 2026-09-06
- **feat(cutscene):** morning оживлена — солнце встаёт из-за леса (новый спрайт sunrise), костёр тлеет и дымит (fx glow+smoke), туристы просыпаются (умываются у воды, помогают со спуском лодки), герой сворачивает палатку, несёт рюкзак к лодке и садится; добавлена река (water) — лодка с гребцом на воду и по течению

### 2026-09-07
- **feat(content):** пляж Дня 1 приведён к финальному виду — волейбол (сетка/игроки/мяч) убран, песчаная полоса и купающиеся в воде; финиш получил большой ровный берег через сужение и уход реки (`finishGain`), лагерь компактнее; утки выглядят утками и взлетают только вплотную (подробности ниже)
- **feat(cutscene):** morning перестроена по каркасу ночи — небо/зелень/река отдельными пластами (gy 216, water 468), рассвет вместо звёзд, те же ели и палатки, байдарки кучей на берегу + одна на воде; герой идёт от костра к байдарке и берёт весло (весло В РУКАХ, поверх туловища)
- **feat(art):** ель (fir) перерисована — ярусный конус из четырёх «этажей» лап со ступенчатой кромкой и стволом, читается как дерево, а не вытянутая пирамида
- **feat(feel):** восход замедлен — первая треть мульта солнце только выглядывает краем из-за елей, к финалу выходит на небо
- **feat(audio):** пение птиц на рассвете — детерминированный синтез WebAudio (чирики, свисты), играет только в morning, выключается вместе с музыкой
- **feat(audio):** основная мелодия в morning-катсцене убрана — на рассвете звучат только птицы (musicTick глушится для cut=morning, ночь/лейв/драйв без изменений)
- **feat(art):** единый спрайт `person_paddle` — герой с веслом в руках (не наложение person+oar); при взятии весла кадр кросфейдится вместо ходящего героя
- **fix(cutscene):** «Под мостом» — добавлена река (water + рябь), опоры стоят в воде и проносятся ЗА лодкой (а не сквозь неё), лодка покачивается на волнах; поезд из сцены убран, подпись исправлена
- **fix(art):** мост в «Под мостом» теперь ЕДИНЫЙ спрайт `bridge` (настил+ферма+две опоры в воду одной картой 110×34) вместо кучи разрозненных pier/deck/truss-актёров, которые читались как «камни в воздухе»
- **feat(art):** единый спрайт `kayak_man` — гребец сидит в байдарке с двухлопастным веслом (не наложение kayak_side+person_sit), корпус посажен в воду, гребец и весло видны целиком
- **fix(gameplay):** бревно/ветка/соперник больше не «перепрыгивают» на другой борт камня при расталкивании у границы русла — старое `separate()` телепортировало объект через препятствие (с одного берега на другой за один кадр), теперь он скользит к краю и останавливается (red-green: 0.52→0.888, без флипа)
- **feat(fight):** драка вёслами анимирована пиксельными кадрами `rower_wind`/`rower_strike`/`rower_flip` (корпус общий с `rower`, те же варианты одежды) вместо процедурных прямоугольников; `fightFrame()` выбирает кадр по фазе замаха, окно парирования осталось мигающей рамкой
- **feat(fight):** парирование больше не удаляет соперника мгновенно — лодка переворачивается (`rower_flip`), дрейфует FLIP_FRAMES кадров и тонет с плеском; перевёрнутая не агрессит и не рулит (red-green: 4 теста, `tests/paddle-fight.test.mjs`)
- **chore(gates):** качественные гейты на проекте — `package.json` (lint/test/test:affected/analyze), VM-харнесс `tests/harness.mjs`, линтер SPR/CUTS, базлайн размера; хуки pre-commit (lint) и pre-push (test)
- **feat(debug):** `?day=N` — мгновенный прыжок в конкретный день при запуске (`index.html?day=4`), дни с дракой видно сразу; `dayFromParam()` протестирован
- **feat(fight):** агрессивные соперники помечены красным вымпелом с самого момента появления — до сближения видно, кто замахнётся (спокойные гребут с веслом в руках)
- **fix(gameplay):** спавн препятствий/бонусов/агрессоров унесён за верхнюю кромку дальше (260px вместо 60px) — объекты вплывают в кадр «с плеса», а не материализуются у края экрана
- **fix(render):** z-порядок препятствий — жёлтая мель и зелёная ряска рисуются ПОД всем плавающим (корабли, бревна, деревья, байдарки их перекрывают), остальные объекты — зад-на-перед по глубине (`drawOrderOf`, протестирован)
- **feat(content):** лента песка в сцене пляжа идёт ВДОЛЬ берега (тянется по y параллельно кромке воды), а не поперёк вглубь — влажный песок у линии воды, полотенца и зонтик в ряд по берегу, купающиеся в воде разнесены вдоль кромки; волейбол убран
- **feat(content):** `BEV_POOLS` — пул береговых актёлов по рекам вынесен в массив для редактирования; `spBev` читает из него, на Волге появились пляжные сцены
- **feat(content):** лагерь у финиша снова компактный (7 актёров, как в исходнике) — большой ровный участок земли даёт РЕЛЬЕФ: `finishGain(w)` (рампа от `level().len`, с len-250 до len+900) плавно сужает реку до 60% и уводит ось к противоположному берегу (до 68px), со стороны лагеря — широкий сухой берег; `ARRIVALS.camp` 30 → 7 актёров, `ARRIVAL_OPP` 8 → 4; рампа начинается задолго до ленты, поэтому при её появлении сцена НЕ прыгает (тест «финиш НЕ дёргается»)
- **feat(art):** утки перерисованы с чёрных силуэтов на маллярдов — зелёная голова, белая щека, оранжевый клюв (`duck`); разлёт — светлые утки с крыльями вверх/вниз (`duckfly0/duckfly1`); порог срабатывания снижен 400 → 95px (одна лодка вплотную)


- **fix(placement):** береговые актёры (люди, костры, палатки у финиша и т.д.) больше не висят на воде и не уезжают за кадр: `bankX()` клампит позицию в [18, W-18] и не ближе 5px к кромке воды; если суша на этой стороне не видна (кромка за краем экрана) — актёр не рисуется вовсе (`bankVisible`); из пляжа убрана подвижка-шифт (именно она вдавливала «косу» в реку), песок — только полоса вдоль кромки
- **fix(ducks):** утки всегда на воде у кромки (на сушу не выходят); при сближении байдарки взлетают — взмахи крыльев (светлые `duckfly0/duckfly1`), стая уходит вверх и к берегу — и убираются через новый `updBev` (вызывается в цикле уборки берегов); срываются только вплотную (95px, одна лодка)
- red-green `tests/bevy-placement.test.mjs` (5 тестов); полный прогон 23/23, lint OK, analyze +3.92%, render-check + check-spr OK
- Итог раунда (пляж без волейбола / большой берег финиша / утки): `tests/day1-beach.test.mjs` и `tests/day1-camp.test.mjs` переписаны, добавлен `tests/finish-terrain.test.mjs` и тест на дистанцию уток; полный прогон 26/26, lint OK, analyze +3.78%, render-check + check-spr OK
- **feat(debug):** режим просмотра сцен без прохождения уровня — `?day=N&scene=beach|finish|camp|ducks` ставит камеру прямо на картинку (пляж, финиш с лагерем, утки, палаточный городок) и мгновенно спавнит её; `?day=N&at=M` телепортирует на произвольный метр маршрута; клавиша **P** скачивает текущий кадр в PNG (`day1-beach-42.png`); пауза перенесена на Escape. Базлайн analyze обновлён под дебаг-код (рост 5.34%, с согласия). 6 тестов, `tests/scene-viewer.test.mjs`; полный прогон 32/32, lint OK, analyze 0.00%, render-check OK
- **fix(рельеф):** Волга больше не «сужается на пол-уровня» — финишная рампа сдвигается к самому концу (len-120, длина 380px) и жмёт реку лишь на -22%; широкий берег причаливания даёт в основном сдвиг оси (72px). `tests/finish-terrain.test.mjs` переписан под «река остаётся широкой, берег открывает сдвиг оси»
- **fix(beach):** отмель перестала быть «квадратом» — мягкий заострённый боб с волнистыми краями, градиентом песок→жёлто-зелёный и зелёными пучками на стыке с травой (плавный переход). Купающихся теперь ДВОЕ, БЕЗ одежды, по грудь в воде («наполовину видны»): голова+плечи+машущая рука, низ скрыт полупрозрачной водой. `tests/day1-beach.test.mjs` переписан (геометрия отмели по path-записям, 5 тестов); `tests/bevy-placement.test.mjs` вынесен из финишной зоны и пишет path-записи. Пакет: lint OK, 33/33, analyze +0.72%, render-check + check-spr OK
- **fix(сцена):** река больше не уходит за экран — `centerAt` зажимает ось (мин. 2px до кромки), даже при полной финишной рамке + сильных синусах поворота русло полностью в кадре. Песок пляжа стал СТАТИЧНЫМ в мире: волнистость краёв и сужение концов считаются по мировому `e.wy` (проекция через `screenYOf`), а не по экранным координатам — при прокрутке камеры отмель переносится ровно на прокрутку и не «ползёт». +2 регрессии: «река в кадре» (`finish-terrain`) и «отмель статична при прокрутке» (`day1-beach`). Пакет: lint OK, 35/35, analyze +0.96%, render-check + check-spr OK
- **fix(ducks):** утки разлетаются только с их стороны — к продольной близости добавлено боковое условие (`g.t * e.side > 0.45`): гребец ушёл больше чем на полрусла к стаиному берегу. Плывёшь серединой или противоположным берегом — стая спокойно качается дальше; новая регрессия «утки не пугаются по середине/чужому берегу»
- **fix(bev):** береговые актёры стоят НА БЕРЕГУ, а не в воде — `bankAnchor()` держит якорь не ближе 54px от кромки (рыбак — 26px, чтобы удочка дотягивалась до поплавка) поверх прежнего screen-клампа; переведены все ветки: медведь, рыбак, костёр, гитара, палатки, дома, церковь, кусты
- **feat(art):** компания у костра сужена — было «четверо» (огонь + двое на ±34 + стоящий), стало ТРОЕ (огонь + гитарист + один сидящий ближе к берегу); группа читается ~на 30% уже и при любом береге целиком на суше
- **fix(render):** байдарка не режется краем экрана, когда русло прижато к кромке холста — `lateralBounds(wy)` ограничивает боковую координату так, что корпус (63px) целиком в [18, W-18]; кламп в `stepLateral` после TMAX. Раньше на участках с завалом оси корпус вылезал за край на ~20px — «видно на половину». Red-green: `tests/kayak-screen.test.mjs` (2 теста), плюс регрессии «группа на суше» и «утки по стороне» в `tests/bevy-placement.test.mjs`. Пакет: lint OK, 39/39, analyze +1.61%, render-check + check-spr OK
- **fix(art):** компания у костра — стало ДВОЕ: сидящий гитарист и стоящий музыкант (на шаг вглубь берега); компактно, при любом береге целиком на суше
- **revert(bev):** береговые актёры снова у кромки как на основном сайте — `bankAnchor` (отступ 54px/26px из предыдущего раунда) удалён, все ветки снова на `bankX` (кромка 5px + кламп [18, W-18]); безопасно из-за суженной группы у костра; в экспорт добавлены `bankX`/`bankVisible`. Тест «компания на суше» остаётся зелёным (wy=0, off=40)
- **feat(beach):** песчаный берег — ТРИ варианта (`e.variant`): 0 короткая отмель + один загорающий, 1 средняя + двое загорающих + зонтик, 2 длинная + двое загорающих. Купающихся В ВОДЕ больше нет — вместо них на КОВРИКАХ лежат ДВОЕ загорающих БЕЗ одежды (силуэт лицом вниз: спина, руки, ноги, макушка волос; головой к берегу). Длина отмели вариантная (72/112/158px мировой), геометрия остаётся мировой (статика от прокрутки сохранена). `spBev` случайно выбирает вариант для beach; дебаг `?scene=beach&v=0|1|2`. `tests/day1-beach.test.mjs` переписан. Пакет: lint OK, 41/41, analyze +1.39%, render-check + check-spr OK

### 2026-09-09
- **feat(beach):** загорающие на ковриках стали КРУПНЫМИ (диаметр головы >10px, коврик ≥40px) — люди читаются в полный рост на песке, а не точками
- **feat(beach):** вернулся КЛАССИЧЕСКИЙ пляж (вариант 3, как на основном сайте): полотенца, зонтик, отдыхающий и ДВОЕ ДЕТЕЙ В ВОДЕ по грудь (низ скрыт полупрозрачной водой, один машет рукой с брызгами); `spBev` больше не выкинул детей — выбор 0..3, дефолт (variant без значения) — классика
- **fix(bev):** береговые актёры снова НА СУШЕ при любом береге — точные отступы от кромки вместо глобальных: медведь 30px, рыбак 27px (удочка до поплавка), гитара 24px, костёр 16px, палатки 44px, остальные — полспрайта + 3px; в песке/гитаре контакт с водой исключён геометрией якорей
- **fix(bev):** `spriteSize` больше не вызывается для «beach» (нет спрайта) — особый случай в `bankAnchor` margin (исправило краши рендера пляжа)
- **feat(art):** гитарный дуэт — сидящий гитарист и стоящий музыкант стоят шагом вглубь берега от кромки
- `tests/day1-beach.test.mjs` — 8 тестов: 3 варианта длины, N загорающих+размер, детей нет (0-2), классика v3 (2 головы в воде, вода-по-грудь, полотенца, зонтик), коврики, статика. Пакет: lint OK, 42/42, analyze +2.52% (405659), render-check + check-spr OK, дебаг `?scene=beach&v=0..3`
- **fix(bev):** повторный визуальный аудит берегов (софт-растеризатор + ASCII): на УЗКОЙ полосе берега (кромка вплотную к краю экрана, напр. левый берег Волги у wy=1000) внешний зажим `Math.max(18,…)` в `bankAnchor` перебивал margin кромки и выталкивал спрайт В ВОДУ (медведь, вышка, дерево, куст — полностью, гитарист 182px, палатка 486px). Фикс: внешний зажим идёт только в сторону суши (`max(0,x)` для левого берега, `min(W,x)` для правого) — актёр может слегка обрезаться краем экрана, но не стоит в реке; на широких берегах поведение не изменилось. Итог по pixel-аудиту: сухие на левом и правом берегу — костёр, медведь, вышка, куст, дерево; гитара 182→15px; рыбак — только удочка/леска над водой (по замыслу); утки — плавают.
- **test(bev):** `bevy-placement.test.mjs` +1 — `bankAnchor` на узком берегу не выталкивает спрайт в воду (красный до фикса: 18+24=42 > edg=29). Пакет: lint OK, 43/43, analyze +2.61% (406043), render-check + check-spr OK
- **fix(fiz):** на извилистой реке байдарка снова ДОПЛЫВАЕТ до берега — `lateralBounds` перестал быть «экранным стражником». Раньше клэмп по краям холста останавливал лодку за 15-20px до уреза воды, когда изгиб прижимал русло к кромке («экран не пускает», левый берег часто не достать). Теперь предел движения — та граница, что дальше: кромка экрана ИЛИ урез воды (`min/max` по `bankX ± pad`). Ось русла (`centerAt`) сама зажата в `[2+hw, W-2-hw]`, поэтому причаливание к урезу держит корпус целиком в кадре; там, где река шире кадра, предел остаётся экранным — бонусы у дальнего берега (t до ±0.75) не отрезаны. Пробой по трём руслам зазор до обоих урезов: 0px (было до 16px)
- **test(fiz):** `kayak-screen.test.mjs` переписан под новую семантику: дожим к ЛЕВОМУ и ПРАВОМУ урезу на сильном завале русла (корпус доходит до уреза и не выходит за кромку) + на широкой центрированной реке предел остаётся экранным (бонусы досягаемы). Пакет: lint OK, 44/44, analyze +2.81% (406822), render-check + check-spr OK
- **fix(рельеф):** русло перестроено так, что к ЛЮБОМУ берегу можно причалить вплотную и байдарка целиком в кадре — маржа берега в `centerSines` повышена 4px → 40px: урез воды держится не ближе ~30px к кромке холста весь сплав (кроме последних ~120px финишной рампы). Раньше на Волге левый урез прижимался к самому краю экрана (2-30px), справа оставалась полоса 80+px — «слева берег заметно уже». Волга теперь прямая большая вода (её характер), узкие реки сохраняют извилистость в рамках маржи.
- **fix(fiz):** убрана причина «слева не пускает» у финиша — центробежный снос теперь считается ТОЛЬКО от изгибов (`curvBend`, secondDiff от `centerSines`), без биения финишной рампы. Плоская `finishGain` в точке старта рампы (len-120) давала ложный импульс кривизны ~-18, снос перебивал вёсла, и дожим влево заканчивал на t=+0.94 (лодку утаскивало вправо). Теперь на любой точке дистанции дожим доходит до берега; к финишу гребец рулит сам.
- **feat(bev):** микросцены (пляж, гитаристы, лагеря, утки, медведи) спавнятся на БОЛЕЕ ШИРОКОМ берегу — новый `widerBevSide(wy, pref)` выбирает сторону с большей полосой суши между урезом и кромкой холста (паритет → `pref`). Раньше случайная сторона сажала сцену на узкую полосу, где она резалась краем кадра; работает и в `spBev`, и в дебаге `?scene=beach|ducks&at=M`. Сцены причаливания (лагерь у ленты) сохранены на своих берегах — там сторона уже выбрана рельефом рампы.
- **test(banks):** `tests/river-banks.test.mjs` — 3 регрессии: оба уреза не ближе 24px к кромке (день 1, кроме последних ~130px); к урезу можно доехать ВПЛОТНУЮ с любой стороны на всём сплаве; у старта финишной рампы дожим влево доводит до берега (красный до фикса: t=0.94). Пакет: lint OK, 47/47, analyze +3.61% (409977), render-check + check-spr OK
- **revert(art):** прибережная полоса «трава-песок» в `drwBg` откачена к узкой 6px-кайме песка у кромки — вариант с полосой пол-лодки (песок 14px + сухая трава до 31px) визуально не подошёл. Русло (маржа 40px), снос от извилин и `widerBevSide` остались.
